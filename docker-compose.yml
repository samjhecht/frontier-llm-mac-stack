# Auto-generated docker-compose.yml - DO NOT EDIT
# Generated by stack-select.sh for stack: ollama
# To change stacks, run: ./stack-select.sh <stack-name>
# 
# This is a placeholder file. The actual configuration is loaded via:
# docker-compose -f stacks/ollama/docker-compose.yml \
#                -f stacks/common/monitoring/docker-compose.yml \
#                -f stacks/common/nginx/docker-compose.yml

version: '3.8'

# Create the shared network
networks:
  frontier-llm-network:
    name: frontier-llm-network
    driver: bridge

# Placeholder services
services:
  placeholder:
    image: alpine:latest
    command: echo "Please use ./start.sh to run the stack"
    profiles:
      - never-run
